server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/business_os
    username: bos
    password: bos_dev_2024
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB
app:
  upload:
    dir: ./uploads
  jwt:
    secret: ${JWT_SECRET:bos_jwt_secret_dev_2024_abcdefghijklmnopqrstuvwxyz1234567890}
    expiration-hours: 24
  ai-engine:
    url: ${AI_ENGINE_URL:http://localhost:8081}
